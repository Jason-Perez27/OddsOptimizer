[pytest]
# Makes "from src..." imports work regardless of how pytest is invoked
# (console script vs `python -m pytest`) by putting the project root on
# sys.path before test collection.
pythonpath = .
testpaths = tests
