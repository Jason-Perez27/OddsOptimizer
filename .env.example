# Copy this file to .env (gitignored) and fill in your real values.
# Never put real secrets in this file -- it's the template that gets committed.

# No API keys are currently required. Underdog Fantasy's public pick'em
# feed (src/data/underdog_lines.py) needs no key, and pybaseball /
# MLB-StatsAPI are also key-free. This file is kept as a placeholder for
# any future credentials.
